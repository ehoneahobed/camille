# Static prototype (pre–Next.js)

This folder preserves the original **HTML + Babel in-browser** UI used to explore product and layout.

- Open `index.html` in a browser (requires network for CDNs: React, Tailwind, Three.js, Ready Player Me).

The production app lives at the repository root (Next.js). See `docs/IMPLEMENTATION_PLAN.md` for build tasks.
